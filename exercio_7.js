//Exercios 5

function calculadora(salario){
     
    sm = 1293.20
    sm = Number(sm);

    equivalente = salario / sm;
    alert(`Você receber o equivalente á ${equivalente} Slario Minimo`) 
    
}   newFunction(salario);

function salario(_calculadora) {
    salario = prompt("Digite o seu salario: ");
    salario = Number(salario);
}
